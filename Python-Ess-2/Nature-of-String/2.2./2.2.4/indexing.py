'''
   
   Python strings are sequences. strings are not list but 
   you can treat them like list in many particular cases

'''

# Example indexing strings 

the_string = "silly walks"

for ix in range(len(the_string)):
    print(the_string[ix], end=' ')

print( )
